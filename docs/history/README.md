# History

Internal planning documents from the build. Kept for provenance, not maintained.

- `DESIGN-BRIEF.md` — the original 47-item framework. Written under the placeholder name
  "compact"; the identifier is `tokendog` everywhere in the code.
- `BUILD-HISTORY.md` — how the slices were built and the decisions taken along the way.
- `plans/`, `specs/` — per-slice specs and plans.

Nothing here describes current behaviour better than the code, `CLAUDE.md`, or `tokendog-docs/`.
